apiVersion: v1
kind: Service
metadata:
  name: {{ include "waf.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "waf.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
