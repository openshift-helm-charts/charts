apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "waf.fullname" . }}-test"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: registry.access.redhat.com/ubi8/ubi
      command: ['echo']
      args: ['test passed']
  restartPolicy: Never
