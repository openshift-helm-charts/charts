apiVersion: v1
kind: Service
metadata:
  name: {{ include "prophaze-waf.fullname" . }}
  labels:
    {{- include "prophaze-waf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prophaze-waf.selectorLabels" . | nindent 4 }}
