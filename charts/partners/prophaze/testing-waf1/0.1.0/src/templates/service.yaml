apiVersion: v1
kind: Service
metadata:
  name: {{ include "testing-waf1.fullname" . }}
  labels:
    {{- include "testing-waf1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testing-waf1.selectorLabels" . | nindent 4 }}
