apiVersion: v1
kind: Pod
metadata:
  name: "nodered-test-connection"
  labels:
    app: node-red-app
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: registry.connect.redhat.com/ibm-edge/node-red:v0.0.3
      command: ['wget']
      args: ['"nodered-chart":{{ .Values.service.port }}']
  restartPolicy: Never
