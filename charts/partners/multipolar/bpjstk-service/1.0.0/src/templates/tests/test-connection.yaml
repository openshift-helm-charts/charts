apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bpjstk-helm.fullname" . }}-test-connection"
  labels:
    {{- include "bpjstk-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bpjstk-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
