apiVersion: v1
kind: Secret
metadata:
  name: {{ include "a10tkc.fullname" . }}-secret
type: kubernetes.io/basic-auth
data:
  username: {{ .Values.secret.username | b64enc | quote }}
  password: {{ .Values.secret.password | b64enc | quote }}
