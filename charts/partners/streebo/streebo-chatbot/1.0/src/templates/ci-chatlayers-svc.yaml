apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.cilayers }}
spec:
  type: ClusterIP
  selector:
    component: {{ .Values.components.cilayers.label }}
  ports:
    - port: {{ .Values.components.cilayers.port }}
      targetPort: {{ .Values.components.cilayers.port }}
  