apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.ciservices }}
spec:
  type: ClusterIP
  selector:
    component: {{ .Values.components.ciservices.label }}
  ports:
    - port: {{ .Values.components.ciservices.port }}
      targetPort: {{ .Values.components.ciservices.port }}
  