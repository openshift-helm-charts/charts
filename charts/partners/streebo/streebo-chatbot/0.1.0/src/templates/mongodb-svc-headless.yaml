apiVersion: v1
kind: Service
metadata:
  name:  {{ .Values.services.mongodb }}
spec:
  ports:
    - port: {{ .Values.components.mongodb.port }}
      targetPort: {{ .Values.components.mongodb.port }}
  clusterIP: None
  selector:
    component: {{ .Values.components.mongodb.label }}