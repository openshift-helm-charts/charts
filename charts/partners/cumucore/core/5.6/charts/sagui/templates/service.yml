apiVersion: v1
kind: Service
metadata:
  name: sagui-service
spec:
  type: NodePort
  selector:
    app: sagui
  ports:
  - name: http
    port: 4000
    protocol: TCP
    nodePort: 30010
    targetPort: 4000
  - name: https
    port: 8000
    protocol: TCP
    nodePort: 30011
    targetPort: 8000