apiVersion: v1
kind: Service
metadata:
  name: smf-service
spec:
  selector:
    app: smf
  ports:
  - name: tcp-port
    protocol: TCP
    port: 4001
    targetPort: 4001
  - name: udp-port
    protocol: UDP
    port: 8805
    targetPort: 8805
  type: ClusterIP