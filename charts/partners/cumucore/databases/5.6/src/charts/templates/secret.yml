apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.database_secrets.mongodb_secrets.name }}
type: {{ .Values.database_secrets.mongodb_secrets.type }}
data:
  MONGO_INITDB_ROOT_PASSWORD: {{ .Values.database_secrets.mongodb_secrets.data.MONGO_INITDB_ROOT_PASSWORD }}
stringData:
  MONGO_INITDB_ROOT_USERNAME: {{ .Values.database_secrets.mongodb_secrets.data.MONGO_INITDB_ROOT_USERNAME }}