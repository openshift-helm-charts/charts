apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: {{ .Release.Name }}
      image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
      command: [ curl 127.0.0.1:80 -v ] 
  restartPolicy: Never
