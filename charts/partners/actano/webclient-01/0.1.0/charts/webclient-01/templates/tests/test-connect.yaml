apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-test"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: {{ .Release.Name }}
      image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
      #command: [ oc get service webclient-01 || kubectl get service webclient-01 ]
      command: [ curl http://webclient-01.default ]
  restartPolicy: Never
