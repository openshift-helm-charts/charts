apiVersion: apps/v1
kind: Pod
metadata:
  annotations:
    "helm.sh/hook": test
  name: sagent
spec:
  containers:
    - name: ls
      image: registry.access.redhat.com/redhat-openjdk-18/openjdk18-openshift:1.15-1
      command: ['ls']
      args: []
  restartPolicy: Never
