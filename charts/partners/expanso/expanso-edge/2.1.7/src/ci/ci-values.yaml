# CI values for chart-testing
# Disables health probes so the chart installs successfully in CI
# without a live Expanso bootstrap connection.

livenessProbe:
  enabled: false

readinessProbe:
  enabled: false
